# The Edge Grid - all tracks. Install via ./setup.sh (it handles the fastecdsa
# build that py-libp2p's pin requires); plain `pip install -r` works only if
# libgmp-dev is already present.

# --- p2p networking (discovery + market protocol) ---
libp2p==0.7.0
trio>=0.26.0
multiaddr>=0.2.0

# --- identity / settlement ---
eth-keys>=0.5.1
eth-utils>=4.1.1
web3>=6.20.0

# --- inference + verification ---
requests>=2.31.0
httpx>=0.27.0
psutil>=5.9.0
groq>=0.11.0

# --- gateway ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- shared ---
pydantic>=2.9.0
python-dotenv>=1.0.0

# --- analysis / reporting ---
pandas>=2.2.0
matplotlib>=3.9.0

# --- tests ---
pytest>=8.3.0
pytest-trio>=0.8.0
python-docx>=1.1.0
